export enum CONTROLS {
  forward = "forward",
  backward = "backward",
  leftward = "leftward",
  rightward = "rightward",
  jump = "jump",
}

export enum PHASE {
  READY = "READY",
  PLAYING = "PLAYING",
  ENDED = "ENDED",
}
